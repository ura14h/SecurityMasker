# 0.1.0 release candidate検証記録

実行日: 2026-07-26

この文書は、schema v1への変更前にapplication version`0.1.0`で実行したhistorical evidenceです。
将来の`1.0.0` release evidenceではありません。

## Source

- macOS arm64 Python 3.11: cleanな一時展開から`PYTHON_COMMAND`指定のtest setupが成功
- macOS arm64 Python 3.12: clean setupが成功
- Linux arm64 Python 3.12: clean setupが成功
- Linuxで公式CPU版Torch `2.13.0+cpu`を選択し、CUDA runtimeへ依存しないことを確認
- ruff、mypy: 成功
- 固定NER必須unit/evaluation: 586件成功
- mock upstream live Gateway: 3件成功
- Linux arm64、外部networkなしの実CLI E2E: 2件成功
- Codex CLI `0.145.0`、Claude Code `2.1.212`
- 実providerへtest bodyを送信していない

## Source archive

- 展開後のsetup、init、validate、NER preview、client config生成が成功
- 別clean worktreeから生成したarchiveがbyte-for-byte一致
- DB/key pairのbackup/restore、不一致拒否、再起動をまたぐTTLを検証

## One-file technical spike

対象: macOS arm64、Python 3.12.13、PyInstaller 6.21.0

| 項目 | 結果 |
|---|---|
| artifact | arm64 thin Mach-O、961,117,984 bytes（約917 MiB） |
| clean build | 243.6秒 |
| cold `--help` | 約25.5秒 |
| warm config-check | 約11.5秒 |
| NER preview | 約46.8秒 |
| 外部runtime link | macOS標準libSystem/libzのみ |
| 署名 | ad-hoc |
| binary E2E | 3件成功 |

この結果だけではbinaryを公開可能と判断しません。現在のblockerは
[開発・リリース状況](../status.md)を参照してください。
